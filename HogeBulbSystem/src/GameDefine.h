#pragma once
#include "SystemDefine.h"

namespace GDEF
{
	static const int imageSize(16);
	static const int mapX(SYSDEF::SizeX / GDEF::imageSize);
	static const int mapY(SYSDEF::SizeY / GDEF::imageSize);
}